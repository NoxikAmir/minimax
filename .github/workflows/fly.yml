name: Fly Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: superfly/fly-deploy-v2@main
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
